#include "common/shaders_common.h"

[[vk::binding(0, 3)]] Texture2D<float4>      linearRT;
[[vk::binding(1, 3)]] RWTexture2D<float4>    encodedOutput;

struct PassUniform
{
    vec4 useEncoded_exposure;
};
DeclareConstantBuffer(PassUniform, passUniform, 3, 2);

float GetUseEncoded() { return passUniform.useEncoded_exposure.x; }
float GetExposure() { return passUniform.useEncoded_exposure.y; }


float3 ACESFilm(vec3 x)
{
    float a = 2.51f;
    float b = 0.03f;
    float c = 2.43f;
    float d = 0.59f;
    float e = 0.14f;
    return saturate((x*(a*x+b))/(x*(c*x+d)+e));
}

[numthreads(8, 8, 1)]
void main()
{
    int2 coords = ivec2(gl_GlobalInvocationID.xy);

    float3 linearColor = linearRT.Load(coords).rgb;
    float3 encoded = ACESFilm(linearColor * GetExposure());

    vec3 result = lerp(linearColor, encoded, GetUseEncoded());

    encodedOutput[coords] = float4(result, 1.0));
}